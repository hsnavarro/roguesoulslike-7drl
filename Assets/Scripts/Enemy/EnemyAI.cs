using UnityEngine;

public class EnemyAI : MonoBehaviour {
  // private float angularVelocity = 20f;

  // private CharacterController enemyController;

  private void Update() {
    // enemyController.Move(new Vector3(0f, 0f, -1f) * Time.deltaTime);
  }
}
